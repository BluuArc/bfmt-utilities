{
  "name": "bf-mt-utilities",
  "version": "0.0.1",
  "description": "Set of utility functions used by the BFMT project. This is a separate repository for them because they are usable outside of BFMT.",
  "main": "dist/index.js",
  "module": "dist-es/index.js",
  "scripts": {
    "test": "jasmine --config=spec/jasmine.json",
    "check-types": "tsc --noEmit",
    "build:types": "tsc --emitDeclarationOnly",
    "build:es": "tsc",
    "build:js": "webpack --mode=production",
    "build": "npm run build:types && npm run build:es && npm run build:js",
    "gulp": "gulp --gulpfile tasks/index.js",
    "clean": "npm run gulp -- cleanup"
  },
  "author": "Joshua Castor",
  "license": "GPL-3.0",
  "devDependencies": {
    "@babel/core": "^7.4.5",
    "@babel/preset-env": "^7.4.5",
    "@babel/preset-typescript": "^7.3.3",
    "@babel/register": "^7.4.4",
    "babel-loader": "^8.0.6",
    "eslint-plugin-jasmine": "^2.10.1",
    "fork-ts-checker-webpack-plugin": "^1.3.7",
    "gulp": "^4.0.2",
    "jasmine": "^3.4.0",
    "typescript": "^3.5.2",
    "webpack": "^4.35.0",
    "webpack-cli": "^3.3.5",
    "webpack-stream": "^5.2.1"
  }
}
